apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: timestamp-formatter
  namespace: timestamp
spec:
  podSelector:
    matchLabels:
      app: timestamp-formatter
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: 8080
